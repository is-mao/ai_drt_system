services:
  web:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - app_data:/app/data
      - ./drt_system.db:/app/drt_system.db
    env_file:
      - .env
    environment:
      - FLASK_DEBUG=0
      - PORT=5001
    restart: unless-stopped

volumes:
  app_data:
