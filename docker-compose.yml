services:
  web:
    build: .
    ports:
      - "5001:5001"
    volumes:
      - app_data:/app/data
      - .:/app
    env_file:
      - .env
    environment:
      - FLASK_DEBUG=0
      - PORT=5001
    restart: unless-stopped

volumes:
  app_data:
